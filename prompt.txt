I need you to add Google Search Console HTML verification to my existing website.
The website is currently deployed at:
https://magikturch.vercel.app/
Google Search Console has provided me with an HTML verification file. I need you to add this verification file to my website correctly without changing, breaking, redesigning, or removing any existing functionality.

What you need to do

Locate the project's public folder.

Add the Google verification HTML file directly inside the public folder.

The verification file must remain at the root of the deployed website, so that Google can access it at:

https://magikturch.vercel.app/[EXACT-GOOGLE-FILENAME].html

Use the exact filename and exact contents of the HTML verification file I provide/downloaded from Google Search Console. Do not rename, modify, recreate, or generate a different verification file.

If the project does not currently have a public folder, create one in the correct location for the framework being used.

Make sure the file is included in the production build and is accessible publicly without authentication, login, redirects, or JavaScript routing interfering with it.

Do not change the existing website design, pages, database, authentication, APIs, SEO settings, or other functionality unless absolutely necessary for this verification.

Deploy the updated website to Vercel.

After deployment, verify that the file is publicly accessible by opening the exact URL:

https://magikturch.vercel.app/[EXACT-GOOGLE-FILENAME].html

Confirm that the URL returns the actual Google verification file contents with a successful HTTP response, rather than a 404 page or the website homepage.

Important

Do not simply tell me how to do this. Actually make the required change in my project.
If you need the Google verification file from me, ask me to upload the downloaded .html file from Google Search Console. Once I provide it, use that exact file.
Do not delete the verification file after verification succeeds. It must remain permanently available because Google may check it again later.
After completing the task, tell me:

The exact filename that was added.

Where it was placed in the project.

The exact public URL where Google can access it.

Whether the deployment was successful.

Whether you confirmed that the verification URL returns the file correctly.